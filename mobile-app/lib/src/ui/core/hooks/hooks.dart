/// Barrel dos hooks reutilizáveis de UI do OBD2 Cockpit.
///
/// Importe `package:tccelta_mobile/src/ui/core/hooks/hooks.dart` para acessar
/// os hooks compartilhados (estado efêmero / ciclo de vida / animação). Estado
/// de negócio continua sendo do Riverpod.
library;

export 'use_loop_controller.dart';
